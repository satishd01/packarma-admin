export const customStyle = {
  control: (provided: any) => ({
    ...provided,
    fontSize: "0.850rem",
    lineHeight: "1.25rem",
  }),
  menu: (provided: any) => ({
    ...provided,
    fontSize: "0.850rem",
    lineHeight: "1.25rem",
  }),
  option: (provided: any) => ({
    ...provided,
    fontSize: "0.850rem",
    lineHeight: "1.25rem",
  }),
};
